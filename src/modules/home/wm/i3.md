# i3

```nix modules/home/wm/i3.nix
{ config, pkgs, lib, inputs, ... }:
{
  imports = [
    ./default.nix
  ];

  xsession.windowManager.i3.enable = true;
  xsession.windowManager.i3.package = pkgs.unstable.i3; # TODO stable after gaps being merged

  <<<modules/home/wm/i3>>>

  home.packages = with pkgs; [
    <<<modules/home/wm/i3-packages>>>
  ];
}
```

```nix "modules/home/wm/i3" +=
home.sessionVariables.XDG_CURRENT_DESKTOP = "i3";
```

## Compositor

```nix "modules/home/wm/i3" +=
services.picom.enable = true;
```

## Clipboard

```nix "modules/home/wm/i3-packages" +=
xclip
```

