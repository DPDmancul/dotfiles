# dotfiles

<center>
  <img width="200px" src="https://gitlab.com/DPDmancul/dotfiles/-/raw/nix/img/icon.png" />
</center>

My dotfiles for NixOS and the Nix package manager, written as a flake.

View the literate [HTML version](https://dpdmancul.gitlab.io/dotfiles).

View the [literate version](https://gitlab.com/DPDmancul/dotfiles/).

# Usage

1. Clone this branch
```sh
git clone -b flake https://gitlab.com/DPDmancul/dotfiles.git
```
2. Optionally run `nix-shell` to get all the required dependencies into the environment
3. Just run `make` to update all dotfiles.

**Warning** read the dotfiles before apply them!

